dependencies {
    implementation project(':core')

    testImplementation project(':test-fixtures')
    testImplementation project(':startup')
    testImplementation project(':adapters-base')
}

bootJar {
    enabled = false
}

jar {
    enabled = true
}